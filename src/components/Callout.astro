---
const { title, tone = 'yellow' } = Astro.props
---

<aside class:list={['callout', `callout-${tone}`]}>
  <span class="callout-pin" aria-hidden="true"></span>
  <h2>{title}</h2>
  <slot />
</aside>
